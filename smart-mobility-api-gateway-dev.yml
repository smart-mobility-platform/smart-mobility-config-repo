# ============================================================
# Profil DEV — API Gateway
# ============================================================

spring:
  cloud:
    gateway:
      server:
        webflux:
          globalcors:
            cors-configurations:
              '[/**]':
                allowedOriginPatterns: "*"

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    reactor.netty: INFO
