# ============================================================
# Profil TEST — Eureka Service Registry
# ============================================================

eureka:
  server:
    enable-self-preservation: false
    wait-time-in-ms-when-sync-empty: 0
  client:
    enabled: false

logging:
  level:
    com.netflix.eureka: WARN
    com.netflix.discovery: WARN
