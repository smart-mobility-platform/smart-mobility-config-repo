# ============================================================
# Profil DEV — Pricing Service
# ============================================================

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pricingdb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    com.smartmobility.pricing: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE
